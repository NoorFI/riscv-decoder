#include "memory.h"
//Empty implementation, again just to check compilation and satisfy linker requirements
int load_hex_file(const char *filename, uint8_t *memory){
    return 0;
}
void memory_dump(const uint8_t *memory, size_t size){}